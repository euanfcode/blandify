# Blandify v0.4

Files:
- `index.html` — the website
- `api/blandify.js` — the secure Vercel server function
- `package.json` — installs the official OpenAI JavaScript package

The Vercel project must have this environment variable:

`OPENAI_API_KEY`

Do not put the secret key in any GitHub file.
